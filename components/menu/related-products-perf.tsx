import React from 'react'
import { Text, View } from 'react-native'

export default function RelatedProducts() {
  return (
    <View className="mt-6 rounded-xl border border-dashed border-gray-200 bg-white p-4 dark:border-gray-700 dark:bg-gray-900">
      <Text className="text-sm text-gray-500 dark:text-gray-400">
        Lazy placeholder block (empty) to keep Suspense/Lazy path in benchmark.
      </Text>
    </View>
  )
}

