export enum LoyaltyPointHistoryType {
  ADD = 'add',
  USE = 'use',
  RESERVE = 'reserve',
  REFUND = 'refund',
}

export enum LoyaltyPointTransactionStatus {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  CANCELLED = 'CANCELLED',
}
