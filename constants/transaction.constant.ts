export enum PointTransactionObjectType {
    ORDER = 'order',
    GIFT_CARD = 'gift-card',
    CARD_ORDER = 'card-order',
  }
  
  export enum PointTransactionType {
    IN = 'in',
    OUT = 'out',
    ALL = 'all',
  }
  